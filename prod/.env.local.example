# NextAuth Configuration
NEXTAUTH_SECRET=dev_secret_for_demo_only_change_in_production
NEXTAUTH_URL=http://localhost:3000

# Database (SQLite - no config needed, file-based)
# DATABASE_URL is in prisma/schema.prisma

# Upload Directory (optional, defaults to ./uploads)
UPLOAD_DIR=./uploads
